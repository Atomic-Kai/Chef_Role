# Chef Panel - Done & Finished with Persistence

- Confirm in **Chef Panel** -> order goes to **Orders** queue (persisted).
- Click **Done** in Orders -> moves to **Finished** page (persisted).
- Uses localStorage key `chefPanelStore.v1`.

## Run
npm install
npm run dev
